//add function will take two inputs (a, b) and add them together
//log the answer

let add = function (a, b) {
  return a + b;
};

// switch add to a default export
// load it in server/main.js as a default export
export default add;
